@model List<DrugStockWeb.Models.Account.User>

@{
    ViewBag.Title = "لیست کاربران";

}
<!-- Modal -->
<div class="modal fade" id="DeleteConfirmationModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                آیا مطمئن هستید؟
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">بستن</button>
                <button type="button" class="btn btn-primary" id="confirmDelete">بله حتما</button>
            </div>
        </div>
    </div>
</div>
@* <button type="button" data-modal- class="btn btn-primary" data-toggle="modal" data-target="#exampleModal"> *@
@*     Launch demo modal *@
@* </button> *@
@* <button type="button" class="btn btn-danger"  data-toggle="modal" data-target="#DeleteConfirmationModal" *@
@* onclick="confirmDeleteModal('10')">Delete Item id 10</button> *@

    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <div id="accordion-one" class="accordion accordion-primary">
                            <div class="accordion__item">
                                <div class="accordion__header rounded-lg" data-toggle="collapse" data-target="#default_collapseOne">
                                    <span class="accordion__header--text">جستجوی پیشرفته</span>

                                    <i class="fa fa-plus " style="float: left"></i>
                                </div>

                                <div id="default_collapseOne" class="collapse accordion__body " data-parent="#accordion-one">
                                    @using (Html.BeginForm(FormMethod.Post))
                                    {
                                        @Html.AntiForgeryToken()

                                        <div style="margin-top: 30px;" class="row form-material">
                                            <div class="col-xl-3 col-xxl-3 col-md-6 mb-3">
                                                <p class="mb-1">نام خانوادگی</p>
                                                <input autocomplete="off" autofill="off" type="text" class="form-control" placeholder="نام خانوادگی" value="" id="LastName" name="LastName">
                                            </div>

                                            <div class="col-xl-4 col-xxl-5 col-md-6 mb-3 vertical-center">
                                                <button type="button" onclick="SearchUser(1);" value="sumbit" name="sumbit" style="margin-right: 15px;" class="btn btn-primary mb-3">اعمال فیلتر</button>
                                                <a href="@Url.Action("UserList")" style="margin-right: 15px;" class="btn btn-outline-primary mb-3">حذف فیلتر</a>
                                            </div>


                                        </div>
                                    }
                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <a href="@Url.Action("Register")" class="btn btn-success" style="width: 250px; margin: 20px;"><li class="fa fa-plus-circle" style="margin-left: 5px;"> </li>ایجاد کاربر جدید</a>
                    @if (ViewBag.ActiveMessage == "activated")
                    {
                        <div class="row">
                            <div class="col-md-12">


                                <div class="alert alert-info help-alert margin-bottom-0"
                                     style="background-color: #8fbc8f; margin-bottom: 15px;color: red">
                                    @*<img src="/Content/Images/logo_light.png">*@
                                    <span>

                                        <label>کاربر با موفقیت فعال شد</label>
                    
                                    </span>

                                    <br>
                                </div>


                            </div>
                        </div>

                    }
                    <div class="card-header bg-primary" style="text-align: center">
                        <h3 class="card-title text-white">لیست کاربران</h3>
                    </div>
                    <div id="UserList">
                        @{
                            Html.RenderPartial("_userList", Model);
                        }
                    </div>

                    <!-- /.card-body -->

                </div>
                <!-- /.card -->
            </div>
        </div>
        <!-- /.row (main row) -->
    </div>

    <div>

    </div>
    @* <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"></script> *@



    @{
        Html.RenderPartial("_sweetAlert");
    }
