@model DrugStockWeb.ViewModels.StoreReport.StockBalanceViewModel

@{
    ViewBag.Title = "لیست موجودی دارو و لوازم";

}
<!-- Modal -->
<div class="modal fade" id="DeleteConfirmationModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                آیا مطمئن هستید؟
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">بستن</button>
                <button type="button" class="btn btn-primary" id="confirmDelete">بله حتما</button>
            </div>
        </div>
    </div>
</div>
@* <button type="button" data-modal- class="btn btn-primary" data-toggle="modal" data-target="#exampleModal"> *@
@*     Launch demo modal *@
@* </button> *@
@* <button type="button" class="btn btn-danger"  data-toggle="modal" data-target="#DeleteConfirmationModal" *@
@* onclick="confirmDeleteModal('10')">Delete Item id 10</button> *@



<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">


                    <div id="default_collapseOne" class=" accordion__body " data-parent="#accordion-one">
                        @using (Html.BeginForm(FormMethod.Post))
                        {
                            @Html.AntiForgeryToken()

                            <div style="margin-top: 30px;" class="row form-material">
                                <div class="col-xl-3 col-xxl-3 col-md-6 mb-3">
                                    <p class="mb-1">نام انبار</p>
                                    @Html.DropDownListFor(p => p.StoreId, Model.StoreList,  new { @onchange = "ChangeStoreInStoreBalance()", @class = "form-control" })
                                </div>
                                <div class="col-xl-4 col-xxl-5 col-md-6 mb-3" >
                                    <div >
                                        <p class="mb-1"> </p>

                                        <button type="button" onclick="SearchProductBalance(1);" value="sumbit" name="sumbit" style="margin-right: 15px;" class="btn btn-primary mb-3">اعمال فیلتر</button>
                                        <a href="@Url.Action("StockBalance")" style="margin-right: 15px;" class="btn btn-outline-primary mb-3">حذف فیلتر</a>
                                    </div>
                                </div>


                            </div>
                        }

                    </div>
                </div>
            </div>
        </div>

    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header bg-primary">
                    <h3 class="card-title text-white">لیست موجودی دارو و لوازم</h3>
                </div>
                <div id="StockBalanceList">
                    @{
                        Html.RenderPartial("_stockBalance", Model.ProductList);
                    }
                </div>

                <!-- /.card-body -->

            </div>
            <!-- /.card -->
        </div>
    </div>
    <!-- /.row (main row) -->
</div>

<div>

</div>
@* <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"></script> *@



@{
    Html.RenderPartial("_sweetAlert");
}