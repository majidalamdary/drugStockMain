@using DrugStockWeb.Utitlities
@model System.Collections.Generic.List<DrugStockWeb.ViewModels.ReceiptReport.ReceiptDetailReportViewModel>
<div class="card-body p-0">
    @if (Model.Any())
    {
        @* <a target="_blank" href="@if (Model != null) {@Url.Action("PrintProductInStock", new { storeId = ViewBag.StoreId }) }" style="margin-right: 15px;" class="btn btn-success mb-3"><i class="fa fa-print"></i> چاپ لیست</a> *@
    }
    <table class="table table-striped table-responsive-sm text-center">
        <thead>
            <tr>
                <th>گروه کالا</th>
                <th>نام کالا</th>
                <th>تاریخ رسید</th>
                <th>شماره رسید</th>
                <th>تعداد</th>
                <th>قیمت خرید</th>
                <th>قیمت فروش</th>
                <th>جمع قیمت خرید</th>
                <th>جمع قیمت فروش</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>

                    <td>@(item.Product.ProductSubGroup.Title)</td>
                    <td>@(item.Product.Title)</td>
                    <td>@item.StoreReceipt.ReceiptDate.ToPersianString(PersianDateTimeFormat.Date)</td>
                    <td>@(item.StoreReceipt.ReceiptNumber)</td>
                    <td>@(item.Count)</td>
                    <td>@(item.BuyPrice.ToString().Cur())</td>
                    <td>@(item.SellPrice.ToString().Cur())</td>
                    <td>@((item.BuyPrice*item.Count).ToString().Cur())</td>
                    <td>@((item.SellPrice*item.Count).ToString().Cur())</td>


                </tr>
            }
        </tbody>
    </table>
    <div class="d-flex justify-content-center">
        <nav class="table-responsive">
            <ul class="pagination justify-content-center d-flex flex-wrap">


                @if (ViewBag.Page > 1)
                {
                    <li class="page-item">
                        <a class="page-link" onclick="SearchReceiptReport(1)">اولین</a>
                    </li>
                    <li class="page-item">
                        <a class="page-link" onclick="SearchReceiptReport(@(ViewBag.Page - 1))">قبلی</a>
                    </li>
                }

                @for (int i = 1; i <= ViewBag.PageCount && i<=10; i++)

                {
                    <li class="page-item @(i == ViewBag.Page ? "active" : "")">
                        <a class="page-link" onclick="SearchReceiptReport(@i);">@i</a>
                    </li>
                }

                @if (ViewBag.Page < ViewBag.PageCount)
                {
                    <li class="page-item">
                        <a class="page-link" onclick="SearchReceiptReport(@(ViewBag.Page + 1))">بعدی</a>
                    </li>
                    <li class="page-item">
                        <a class="page-link" onclick="SearchReceiptReport(@(ViewBag.PageCount))">آخرین</a>
                    </li>
                }
            </ul>
        </nav>
    </div>


</div>

